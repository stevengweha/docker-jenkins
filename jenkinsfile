pipeline {
    agent any  

    environment {
        CONTAINER_ID = ""  // ID du conteneur existant
        SUM_PY_PATH = "./sum.py"  // Chemin du script Python
        DIR_PATH = "./"  // Répertoire contenant le Dockerfile
        TEST_FILE_PATH = "./test_variables.txt"  // Fichier contenant les valeurs de test
    }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/stevengweha/docker-jenkins.git'
            }
        }

        stage('Check Running Container') {
            steps {
                script {
                    def output = sh(script: 'docker ps -q -f ancestor=sum-app', returnStdout: true).trim()

                    if (output.isEmpty()) {
                        error "⚠️ Aucun conteneur existant trouvé pour l’image sum-app ! Lancez-le avant d’exécuter la pipeline."
                    } else {
                        echo "✅ Conteneur existant détecté : ${output}"
                    }
                }
            }
        }

        stage('Test') {
            steps {
                bat 'echo Running tests...'
                // Ajoute ici les commandes pour exécuter tes tests
            }
        }

        stage('Deploy') {
            steps {
                bat 'echo Déploiement en cours...'
                // Ajoute ici les commandes pour le déploiement si nécessaire
            }
        }
    }

    post {
        always {
            script {
                def output = bat(script: 'docker ps -q -f ancestor=sum-app', returnStdout: true).trim()
                if (!output.isEmpty()) {
                    echo "🧹 Nettoyage du conteneur..."
                    bat "docker stop ${output}"
                } else {
                    echo "⚠️ Aucun conteneur trouvé pour nettoyage."
                }
            }
        }
    }
}
